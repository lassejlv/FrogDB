{
  "name": "@frogdb/server",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch .",
    "start": "bun run src/index.ts",
    "compile": "bun build --compile --minify ./src/index.ts --outfile build/server"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.10.1",
    "hono": "^4.2.8",
    "zod": "^3.23.4"
  }
}
